# Formcheck v0.1.0 release publication

This file triggers the verified GitHub Actions release publisher for the first working Windows release.

The publisher rebuilds the validated application, creates or updates the `v0.1.0` GitHub Release, uploads the EXE, MSI, and combined ZIP, and verifies all three asset names before succeeding.
