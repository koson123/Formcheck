# Skill tree overhaul plan

The finished map should use one organic graph rather than boxed columns. Circular movement nodes form directional paths across six major zones, while local progress states distinguish locked, available, in-progress, and mastered skills.
