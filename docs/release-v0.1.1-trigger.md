# Formcheck v0.1.1 release publication

This documentation-only change triggers the inspectable GitHub Actions publisher for the validated skill-roadmap redesign and rep-counting fixes.

The publisher must rebuild the app, pass the false-positive rep tests, create or update the `v0.1.1` Release, and verify the EXE, MSI, and combined ZIP by exact filename.
